// 2 : Add or remove element from array using push and pop

const numbers = [1, 2, 3, 4];
const addNumber = numbers.push(5);
console.log(numbers);
const removeNumber = numbers.pop();
console.log(numbers);